#include <studio.h>
#include <stdlib.h>
#include <math.h>
int main (){

 double x, z, r;
 
 printf("Wpisz liczbę rzeczywistą: ");
 scanf ("%lf", &x);
if (x==0) {
printf("odwrotność 0 z %.2lf nie istnieje\n");  
z=sqrt(x);   /*kompilowac z -lm */
printf("Pierwiastek kwadratowy z %.2lf wynosi:", z);
} else if (x < 0) {
printf("Pierwiastek", r=1/x;
printf("odwrotność liczby %.2lf wynosi:", x);

printf("Pierwiastek", %.2lf;
}
 
 
return 0;
}
